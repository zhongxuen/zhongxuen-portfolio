import { Project } from "@/types/project";

/**
 * Portfolio projects — the single source of truth for which projects exist and
 * for all of their narrative content.
 *
 * WRITTEN BY THE ADMIN CONSOLE. Editing by hand is fine and expected, but the
 * array below is re-emitted in full by lib/admin/serializeProjects.ts on every
 * save from /admin, so **a comment placed inside the array will be deleted by
 * the next one**. Put notes in this header instead. GitHub-derived fields
 * (language, stars, lastUpdated, publishedAt) are overlaid at request time by
 * adapters/githubProjectAdapter.ts and must not be written here.
 */
export const projects: Project[] = [
    {
        slug: "internet-visualizer",
        title: "Internet Visualizer",
        description:
            "A teaching tool that animates how the Internet actually works — DNS, TCP, TLS and HTTP taken apart into simulations you step through rather than paragraphs you have to picture. Ten modules, thirty-nine lessons, every claim cited to an RFC.",
        longDescription:
            "A Next.js app built around a deterministic simulation kernel: every protocol module is a pure, framework-free simulation that emits an ordered list of typed events on a virtual clock, and the UI is only a renderer of that event stream. Nothing under the core imports React or the graph library, and an ESLint boundary rule fails the build if it does — which keeps the protocol logic unit-testable without a browser and lets one shared timeline component (play, pause, step forward, step back, scrub, speed) drive all nine simulating modules rather than each rolling its own animation loop. Eight Explore modules take one protocol apart each, from a network built up machine by machine to a URL composing DNS, TCP, TLS and HTTP into a page load; a Diagnostics module covers ping, traceroute, DNS lookup and WHOIS; and a Learning Center carries thirty-nine lessons across eight tracks plus a sixty-three-term glossary. A lesson does not illustrate a module — it embeds the module's own run through a shared manifest, so a lesson cannot draw a packet that contradicts the one the simulator draws. Accuracy is enforced rather than asserted: running every scenario produces 503 teaching citations naming 45 RFCs and a test fails on any that is malformed or undocumented, a determinism test runs every scenario twice and asserts the two runs are deep-equal, and two more tests hold every colour token above its contrast floor and prove no two node states are indistinguishable in greyscale.",
        technologies: [
            "Next.js",
            "TypeScript",
            "React",
            "Tailwind CSS",
            "Zustand",
            "React Flow",
            "Zod",
            "Playwright",
        ],
        githubUrl: "https://github.com/zhongxuen/internet-visualizer",
        githubRepo: "internet-visualizer",
        liveUrl: "https://internet-visualizer.vercel.app",
        screenshots: ["/images/projects/internet-visualizer-home.jpg"],
        keyFeatures: [
            "Ten modules: eight protocol explorers (network map, packet journey, DNS, HTTP, HTTPS/TLS, APIs, WebSockets, and a full page-load simulator), a diagnostics tool, and a learning centre",
            "Deterministic simulation kernel — protocol logic is pure TypeScript emitting typed events on a virtual clock, with no React or DOM imports",
            "Three architectural boundaries enforced by ESLint rather than convention: the core imports no UI, no module imports another module, no component imports a module",
            "One shared timeline primitive (play, pause, step, scrub, speed) drives every simulating module",
            "Thirty-nine lessons across eight tracks plus a 63-term glossary — each lesson embeds the module's own simulation run, so a lesson cannot contradict the thing it teaches",
            "503 teaching citations naming 45 RFCs, with a test that fails on any citation that is malformed or undocumented",
            "A determinism test runs every scenario twice and asserts the runs are deep-equal, so scrubbing backwards always lands on the same frame",
            "Accessibility tested, not assumed: axe on every route including all thirty-nine lessons, keyboard traversal of the timeline, a focusable list alternative to every canvas, and contrast plus greyscale-distinguishability held by unit tests",
            "The one module that can reach a network does so through a single SSRF-guarded chokepoint, rate-limited, disclosed before it runs, and never persisted",
        ],
        disclaimers: [
            "Everything on screen is a deterministic client-side simulation. The one exception is Network Diagnostics' Live mode, which is off until you switch it on, names the exact URL before requesting it, and is visibly badged the whole time it runs.",
            "There is no cryptography anywhere in the TLS layer — the handshake is modelled, not performed. It is the largest of the deliberate simplifications, and all of them are written down in the repo's docs/ACCURACY.md.",
            "There is no live traceroute: a serverless runtime cannot send ICMP, and faking one would be worse than leaving it out.",
            "The repository has no licence chosen yet, so the source is readable but not cleared for reuse.",
        ],
        featured: true,
        order: 1,
    },
    {
        slug: "hacker-simulation",
        title: "Hacker Simulation",
        description:
            "A story game that teaches cybersecurity from absolute zero — type real commands in a simulated terminal, map a network as you explore it, and learn why written permission comes before any of it. No sign-up, nothing to install.",
        longDescription:
            "A Next.js game for people who have never studied security, networking or Linux, and may never have opened a terminal. You join Candlewright Security, a fictional team of good-guy hackers, as its newest recruit, and work through short missions that have you typing commands in a practice terminal, watching a network light up machine by machine as you map it, and making the calls a professional makes — including waiting for permission when a teammate suggests a shortcut. Underneath it is a pure, deterministic, headless simulation engine: a model of computers, files, users, networks and tools that produces the same output from the same seed and the same commands every time, and imports nothing outside itself. Missions are YAML, lessons are MDX, the glossary and campaign are data, and all of it is validated by Zod schemas — so a new mission is a new file rather than new code. Almost the entire app is static; the only server code is the mentor's three routes, which call Claude Haiku 4.5 through the Anthropic SDK with a key that never leaves the server, and a bundle scan after every build proves it.",
        technologies: [
            "Next.js",
            "TypeScript",
            "React",
            "Tailwind CSS",
            "Zod",
            "Claude API",
            "Vitest",
            "Playwright",
        ],
        githubUrl: "https://github.com/zhongxuen/hacker-simulation",
        githubRepo: "hacker-simulation",
        liveUrl: "https://hacker-simulation.vercel.app",
        keyFeatures: [
            "Campaign chapter of three 8–15 minute story missions: first commands and why permission comes first, reading files and permissions on a server, and mapping a small business network to find the one door that should not be open",
            "A deterministic, headless simulation engine modelling computers, files, users, networks and tools — same seed and commands, same output, every time",
            "Practice terminal with a one-minute tour, plus a sandbox of three machines with no goals and a reset button",
            "Learning Center: 28 plain-language lessons covering Linux, networking, the web, security ideas, and ethics and the law — each with quizzes or a live terminal — plus a 117-word glossary and a manual page for every command",
            "Noor, an AI mentor who gives hints rather than answers, explains any line or error on screen, and reviews a finished mission with you",
            "Missions are YAML and lessons are MDX, both Zod-validated — a new mission is a new file, not new code",
            "Module boundaries enforced by ESLint and tests: the engine imports nothing outside itself, content imports only content",
            "Every page checked with axe on every change, keyboard-only operable, reduced-motion honoured, and contrast-checked colours",
            "A 200 KB per-page JavaScript budget and a post-build scan proving no secret reached the browser",
        ],
        disclaimers: [
            "Every machine, network, terminal and tool is make-believe and runs in your browser. Nothing here touches a real computer, and the tools carry invented names (netscan, not a real scanner) so they cannot be mistaken for the real thing.",
            "It teaches authorised, defensive security only. Every mission states its written permission in the story and closes with what the technique is used for defensively, when it is illegal, and what real authorisation looks like. No real people, companies or addresses appear — only reserved ranges and example domains.",
            "Aimed squarely at complete beginners. It is not a CTF platform, not a pentest lab, and not preparation for a certification.",
            "Content is Chapter 1 only — three missions, plus the sandbox, terminal and Learning Center. Later chapters are not written.",
            "The live mentor needs an Anthropic API key. Without one, Noor still answers, but from hints written ahead of time rather than generated.",
            "No accounts and no database: settings live in your own browser and nowhere else, so clearing site data clears them. Optional anonymous usage counts have an off switch and are never sent from a browser set to Do Not Track.",
        ],
        featured: true,
        order: 2,
    },
    {
        slug: "personal-ai-assistant",
        title: "JARVIS – Personal AI Assistant",
        description:
            "A modular personal assistant with a FastAPI backend and a React dashboard, which routes requests between Gemini and a local Ollama model and answers on the web, on Discord, over WhatsApp, on mobile, and on the desktop.",
        longDescription:
            "A FastAPI service that owns one assistant loop and exposes it through several front doors: a React dashboard deployed to Vercel, a Discord bot that starts inside the backend process rather than as a second service, a WhatsApp webhook, a mobile-facing API client, and a desktop agent. An LLM router sits in front of the providers — Gemini first, with automatic failover to a locally-hosted Ollama model when a quota is exhausted or a provider is unhealthy — so the assistant keeps answering when the cloud model is unavailable. On top of the loop sit a tool registry (files, applications, terminal, clipboard, timers, notifications, tasks, routines), a scheduler for recurring routines and reminders, and a retrieval-backed memory store. The Discord bot can be started and stopped from the dashboard's Settings tab against a live connection status, so toggling it no longer means restarting the backend. The security boundary is explicit and enforced twice: every desktop-only tool declares its platform and is additionally gated at the HTTP layer, so a request claiming to be the desktop that did not arrive from loopback is rejected before any tool runs, and secrets stay on the backend — the frontend build reads exactly one environment variable, and it is a plain URL.",
        technologies: [
            "Python",
            "FastAPI",
            "React",
            "TypeScript",
            "SQLAlchemy",
            "Gemini",
            "Ollama",
        ],
        githubUrl: "https://github.com/zhongxuen/Personal-AI-Assistant",
        githubRepo: "Personal-AI-Assistant",
        liveUrl: "https://personal-ai-assistant-virid.vercel.app",
        keyFeatures: [
            "One assistant core served to a web dashboard, a Discord bot, a WhatsApp webhook, a mobile client, and a desktop agent",
            "LLM router with automatic Gemini → local Ollama failover on quota exhaustion or provider health failure",
            "Tool registry covering files, applications, terminal, clipboard, timers, notifications, tasks and routines",
            "Scheduler driving recurring routines, task reminders and web-push notifications",
            "Retrieval-backed memory store, so context carries across sessions and platforms",
            "Discord bot lives inside the backend process and is started, stopped and monitored from the dashboard's Settings tab rather than by restarting the service",
            "Desktop-only tools double-gated: declared per platform and rejected at the HTTP layer unless the request is from loopback",
            "Split deploy — frontend on Vercel, API on Render — with no secret reachable from the browser bundle",
        ],
        disclaimers: [
            "The deployed dashboard is a private, single-user instance and asks for a sign-in. The live link is there to show it running, not as an open demo.",
            "The Gemini → Ollama failover only has somewhere to fall back to when Ollama is running on the same machine. The hosted deployment has no local model, so there the failover is a no-op.",
            "Desktop-only tools — files, applications, terminal, clipboard — work only with the local desktop agent running on the same machine. The hosted instance rejects them at the HTTP layer regardless of what the request claims.",
            "Every front door is opt-in and keyed: the Discord bot, the WhatsApp webhook and Gemini each stay off until their token or key is set in the environment.",
        ],
        featured: true,
        order: 3,
    },
    {
        slug: "routine-launcher",
        title: "Routine Launcher – Productivity Hub",
        description:
            "A local-first Windows desktop app built with Tauri and Rust that puts routine launching, task management, a focus timer, a calendar feed, app-usage tracking and a progress layer behind one window — no account, no backend, no cloud database.",
        longDescription:
            "A Tauri 2 desktop app: a React and TypeScript frontend over a Rust backend that owns a bundled SQLite database, so planning, starting, working, completing and tracking all happen in one place and the data stays on the machine. A routine is a saved set of applications, files and URLs launched together, so starting work is one click rather than a dozen. Around that sit a task manager with categories, recurrence and reminders, a focus timer with presets, an XP and achievement layer that reads from what was actually completed, disk-cleanup tools, a read-only calendar feed, an optional foreground-application usage tracker, and a phone companion that serves today's tasks to a phone on the same local network. The app runs four separate Tauri windows — the main window, a compact popup, a global-shortcut quick launcher, and an optional transparent desktop widget — which means four webviews and therefore four copies of every store reading one database; windows announce writes to each other and re-read rather than passing rows around, so the database stays the single source of truth. The one thing that cannot travel that way is a running clock, so the focus session gets its own channel that carries the live session itself. Colour is constrained rather than chosen by eye: every token drawn as text clears 4.5:1 in both themes, and the five accent palettes meet the same bar. Ships as a Windows installer, built and signed by a tag-triggered GitHub Actions workflow that also publishes the updater manifest.",
        technologies: ["Tauri", "Rust", "React", "TypeScript", "SQLite", "Tailwind CSS", "Zustand"],
        githubUrl: "https://github.com/zhongxuen/routine-launcher-productivity-system",
        githubRepo: "routine-launcher-productivity-system",
        keyFeatures: [
            "Routines that launch a saved set of apps, files and URLs together in one click, plus shareable .routine.json templates that import with every command action switched off",
            "Task manager with categories, recurrence rules and reminders",
            "Focus timer with presets, kept in sync across windows by a dedicated session channel because a running clock cannot travel as a re-read",
            "XP, quests and achievements derived from completed work, plus a progress and analytics view",
            "Optional application-usage tracking that credits the foreground program every five seconds, skips idle time and the lock screen, and keeps ninety days",
            "Read-only calendar integration from a private .ics feed or an imported file, with RRULE recurrence expanded into local-time occurrences on the day in view",
            "Phone companion: an installable web page served to phones on the same local network, paired by QR code, covering today's tasks and nothing else",
            "Folder-based sync through OneDrive, Dropbox, Google Drive or a network share — a snapshot, never a merge, with conflicts surfaced rather than guessed",
            "Four Tauri windows — main, compact popup, global-shortcut quick launcher, and an optional transparent desktop widget — each with its own narrowed permission set",
            "Disk cleanup for downloads, duplicate files, large files and screenshots; system tray, launch-on-startup, backups and crash logging",
            "Contrast-constrained design tokens and five accent palettes, each held above 4.5:1 for text in both light and dark themes",
            "Windows installer published by a tag-triggered GitHub Actions release workflow, with an updater manifest",
        ],
        disclaimers: [
            "Windows only. It ships as a Windows installer and leans on WebView2 and Windows-specific APIs — there is no macOS or Linux build.",
            "No live demo, because it is a desktop app: the installer is in the repository's installers/ folder and on the tagged releases.",
            "Local-first by design — no account, no backend, no cloud database. The four features that can reach outside the machine (folder sync, the calendar feed, the phone companion and usage tracking) are each off until you switch them on in Settings.",
            "Folder sync is a snapshot, not a merge. It suits one computer at a time; two machines editing at once meet a conflict that waits for you rather than a silent guess.",
            "Usage tracking records only the executable name and path per hour. It never reads window titles, and the table has no column that could hold one.",
            "Two of the five accent themes are cosmetic rewards unlocked at level 3 and level 5. Light, dark, system and the default accent are always available.",
        ],
        featured: true,
        order: 4,
    },
    {
        slug: "ai-code-visualizer",
        title: "AI Code Visualizer",
        description:
            "A step-by-step code execution visualizer for JavaScript, TypeScript, Python and Java — watch variables, call stack, memory and control flow update as you step through code, plus a gallery of built-in algorithm visualizations.",
        longDescription:
            "A Next.js app that parses source code into an AST — acorn and Babel for JavaScript and TypeScript, CPython's own ast module running inside Pyodide for Python, and a tree-sitter grammar for Java — then walks that tree to build a frame-by-frame timeline. Every language is translated into the same ESTree-shaped node vocabulary, which is what lets one execution pipeline drive all four without a line of language-specific code below the parser. A Monaco-based editor drives the whole flow: as you step, synced panels show variable state, call stack, scope and memory, alongside a console output timeline, an interactive React Flow control-flow graph, and a time/space complexity badge. A separate Algorithms mode ships eleven ready-made, steppable visualizations — BFS, DFS, binary search, bubble, insertion and selection sort, Fibonacci and knapsack DP, and tree traversal — with no code to write.",
        technologies: [
            "Next.js",
            "TypeScript",
            "React",
            "Tailwind CSS",
            "Zustand",
            "React Flow",
            "Monaco Editor",
            "tree-sitter",
            "Pyodide",
        ],
        githubUrl: "https://github.com/zhongxuen/ai-code-visualizer",
        githubRepo: "ai-code-visualizer",
        liveUrl: "https://ai-code-visualizer-one.vercel.app",
        keyFeatures: [
            "Monaco-based editor supporting JavaScript, TypeScript, Python and Java through pluggable language adapters",
            "Four parsers normalised to one node vocabulary: acorn and Babel for JS/TS, CPython's own ast module via Pyodide for Python, tree-sitter for Java",
            "Step-by-step playback with synced variable, scope, call stack and memory timelines",
            "Live console output tracked alongside execution steps",
            "Interactive control-flow graph built with React Flow",
            "Time and space complexity analysis surfaced as a Big-O badge",
            "Eleven built-in algorithm visualizations — BFS, DFS, binary search, three sorts, two DP problems and tree traversal — each steppable without writing code",
            "Unsupported syntax degrades rather than crashes: unrecognised expressions fall back to a placeholder and unrecognised statements are dropped from the step list",
        ],
        disclaimers: [
            "Despite the name, there is no AI or LLM anywhere in it. The analysis is entirely static — the code is parsed to a syntax tree and walked. There is no model call, no API key and no backend.",
            "It does not actually execute your code. Steps come from walking the syntax tree, so it shows the shape of execution — control flow, scope, call stack — rather than real computed values.",
            "Loops are unrolled against literal bounds and capped: 1,000 iterations, or 2,000 steps, whichever comes first. A loop past that is marked truncated.",
            "Java support reconstructs structure only. Types, generics, overload resolution and inheritance are not modelled, and interfaces, enums, records, constructors, fields, switch and lambdas are outside the mapped subset — a Java class shows as a bag of methods with no state.",
            "Python coverage is capped to the same feature surface as JavaScript. Decorators, comprehensions, async, match statements and context managers degrade to placeholders rather than stepping.",
            "Python and Java load their WASM runtimes from a CDN on first use, so those two languages need a working network connection even though nothing you write leaves the browser.",
        ],
        featured: true,
        order: 5,
    },
    {
        slug: "it-ticket-helpdesk-system",
        title: "IT Ticket Helpdesk System",
        description:
            "A role-based IT helpdesk platform — admins, IT staff, technicians and employees log, track and resolve support tickets end-to-end, with access boundaries enforced in the database rather than only in the UI.",
        longDescription:
            "A Next.js App Router application on Supabase, covering the full ticket lifecycle: an employee raises a ticket, IT or an admin assigns it to a technician, and status changes, assignments and comments are logged as history rather than overwritten. Comments come in two kinds — public ones the reporter sees, and internal ones visible only to staff roles. Admins additionally manage users, ticket categories and system settings. Access control is not only a UI concern: Supabase row-level security policies enforce the same boundaries at the database, so a role cannot read another's tickets by calling the API directly.",
        testCredentials: {
            password: "12345678",
            accounts: ["admin@gmail.com", "it@gmail.com", "tech@gmail.com", "employee@gmail.com"],
        },
        technologies: [
            "Next.js",
            "TypeScript",
            "Supabase",
            "PostgreSQL",
            "Tailwind CSS",
            "Zustand",
            "React Hook Form",
            "Zod",
        ],
        githubUrl: "https://github.com/zhongxuen/IT-ticket-helpdesk-system",
        githubRepo: "IT-ticket-helpdesk-system",
        liveUrl: "https://it-ticket-helpdesk-system.vercel.app/",
        keyFeatures: [
            "Role-based access control across Admin, IT, Technician and Employee, enforced in Supabase row-level security rather than only in the UI",
            "Full ticket lifecycle: create, assign, update status, comment, resolve and reopen",
            "Public and internal (staff-only) comments on the same ticket",
            "History tracking — every status change and reassignment is logged rather than overwritten",
            "Admin management of users, ticket categories and system settings",
            "Forms validated with React Hook Form and Zod against a strict-mode TypeScript codebase",
            "Profile management and a persisted dark mode",
            "Published demo accounts, one per role, sharing a single password",
        ],
        disclaimers: [
            "The live link is a shared public demo with published credentials. Anything you type into it is visible to anyone else who signs in, and the data may be reset at any time — do not enter anything real.",
            "The repository ships no seeded users. The demo accounts exist only on the deployed instance; a local clone needs its own Supabase project and users created through the admin panel.",
            "Reopening a resolved ticket as an employee creates a new linked ticket rather than reviving the closed one.",
            "There is no email delivery. Notifications and the support-email setting are in-app values, not a configured mail service.",
        ],
        featured: true,
        order: 6,
    },
    {
        slug: "grabexpress-receipt-collector",
        title: "GrabExpress Receipt Collector",
        description:
            "A Next.js web app that signs in to Gmail, finds GrabExpress delivery receipt emails specifically, and exports them as a formatted .xlsx spreadsheet — with a demo mode that runs the whole pipeline without a sign-in.",
        longDescription:
            "Web app, deployed on Vercel, that lets any Gmail user sign in with Google, finds GrabExpress receipt emails by subject line — since those receipts are often forwarded rather than sent directly — parses the receipt data out of the email HTML with cheerio, and downloads the results as a formatted .xlsx file built in memory with exceljs. There is no database, no local token file, no OCR and no PDF scraping: the session lives in an encrypted cookie and the spreadsheet is streamed straight back to the browser. A built-in Demo Mode runs the full parse → dedupe → export pipeline against bundled synthetic sample receipts with no sign-in and no real Gmail access, which matters because the app requests a sensitive OAuth scope that Google gates to approved test users until verification.",
        technologies: ["Next.js", "TypeScript", "NextAuth.js", "Gmail API", "cheerio", "exceljs"],
        githubUrl: "https://github.com/zhongxuen/GrabExpress-Receipt-Collector",
        githubRepo: "GrabExpress-Receipt-Collector",
        liveUrl: "https://grab-express-receipt-collector-sigma.vercel.app/?demo=1",
        screenshots: ["/images/projects/grabexpress-demo.jpg"],
        keyFeatures: [
            "Google sign-in on the read-only Gmail scope, with the session in an encrypted cookie and no database",
            "Finds GrabExpress receipt emails by subject line, which survives the forwarding those receipts usually arrive through",
            'Flexible date ranges: all time, since a date, a specific month, a custom range, or a free-form phrase like "last 90 days"',
            "Parses date, receipt/booking number and amount out of the email HTML via cheerio, with a regex fallback",
            "Deduplicates by Gmail message ID or booking code before export",
            "Streams a formatted .xlsx built in memory with exceljs straight back as a browser download",
            "Skipped or failed messages are reported back in the UI rather than logged silently",
            'Demo Mode runs the real pipeline on bundled sample data with no sign-in, reachable from a "Try the demo" entry point or the /?demo=1 link, with a persistent banner marking demo output',
        ],
        disclaimers: [
            "GrabExpress delivery receipts only. GrabFood, GrabCar, GrabMart and the other Grab services send a different email format and are neither parsed nor collected.",
            "Receipts are matched by subject line, so a forwarded receipt whose subject was edited or renamed will be missed.",
            "Real Gmail sign-in is gated. The sensitive gmail.readonly scope needs Google's verification review, so until that passes the consent screen shows an “unverified app” warning and only approved test users can sign in — the live link goes straight to Demo Mode for that reason.",
            "Read-only and stateless: the Gmail scope is read-only, there is no database and no token file, and the spreadsheet is built in memory and streamed to your browser rather than stored.",
        ],
        featured: true,
        order: 7,
    },
    {
        slug: "jobnow",
        title: "JobNow – Job Listing Application",
        description:
            "A two-sided, swipe-based job matching app for Android, built with React Native (Expo) and Supabase — seekers swipe jobs, companies swipe candidates, and a mutual like opens a direct chat.",
        longDescription:
            "An Expo Router app backed entirely by Supabase, with two account types sharing one swipe deck. A job seeker is shown ranked job cards; a company is shown ranked candidate cards. Ranking is a keyword-overlap score computed in the client — the seeker's skills against the job's required skills, and their stated interests against the job title, each weighted half — surfaced on the card as a percentage match rather than left implicit. A like from both sides creates a match row, which opens a conversation and fires notifications to both parties. Auth, Postgres, row-level security policies, file storage for resumes and profile images, and the messaging tables all live in Supabase; there is no separate backend. The app ships as an Android APK, distributed from a small landing page rather than through the Play Store.",
        role: "University capstone, four-person team",
        technologies: ["React Native", "Expo", "TypeScript", "Supabase", "PostgreSQL"],
        githubUrl: "https://github.com/zhongxuen/JobNow-Application-Capstone-Project",
        githubRepo: "JobNow-Application-Capstone-Project",
        liveUrl: "https://job-now-navy.vercel.app/",
        screenshots: ["/images/projects/jobnow-landing.jpg"],
        keyFeatures: [
            "Two-sided swipe deck: seekers swipe jobs, companies swipe candidates",
            "Keyword-overlap scoring over skills and interests, shown as a percentage match on each card",
            "Mutual like creates a match, which opens a direct conversation",
            "In-app messaging and notifications built on Supabase realtime",
            "Resume and profile-image upload with an in-app document preview",
            "Auth, Postgres and row-level security policies in Supabase — no separate backend",
        ],
        disclaimers: [
            "A university capstone built by a four-person team, not a solo project.",
            "Android only, and distributed as an APK from the landing page rather than through the Play Store — installing it means allowing installs from unknown sources. There is no iOS build.",
            "The live link is the landing page the APK is downloaded from, not a playable web version of the app.",
            "The percentage match is a keyword-overlap score computed on the device, not a machine-learning model.",
        ],
        featured: true,
        order: 8,
    },
    {
        slug: "ecoquest",
        title: "EcoQuest – Environmental Awareness Web App",
        description:
            "A gamified environmental-awareness platform built with PHP and MySQL, where students complete quests, earn badges, and redeem rewards under moderator and admin oversight.",
        longDescription:
            "A three-tier PHP application over a MySQL schema, with separate dashboards for admins, moderators and students. A student takes on quests, submits evidence, earns badges and XP, climbs a leaderboard, and redeems accumulated points for rewards; a moderator reviews those submissions and handles reported forum posts; an admin sits above both, managing quests, badges, rewards, users, announcements and reports. Moderation is a first-class flow rather than an afterthought — submissions, forum reports and bans each have their own review screen and are recorded as moderation records.",
        role: "University group coursework, six contributors",
        technologies: ["PHP", "MySQL", "JavaScript", "CSS"],
        githubUrl: "https://github.com/zhongxuen/EcoQuest-Web-Development",
        githubRepo: "EcoQuest-Web-Development",
        keyFeatures: [
            "Role-based access for Admin, Moderator and Student accounts, each with its own dashboard",
            "Quest, badge and XP system with a leaderboard",
            "Rewards catalogue with point redemption and image uploads",
            "Community forum with post reporting, review screens and recorded moderation actions",
            "Submission review queue shared between moderators and admins",
            "Announcements, notifications, feedback and ban handling",
            "PHP backend over a MySQL schema checked into the repo, with a Dockerfile for local setup",
        ],
        disclaimers: [
            "University group coursework built by six contributors, not a solo project.",
            "No live deployment. Running it needs a PHP server and MySQL, with the bundled ecoquest.sql schema imported first — the repo includes a Dockerfile for that.",
            "Coursework-grade security. It was written to a module brief, not hardened for production, and should not be pointed at real user data.",
            "Uploaded quest evidence and reward images are written to a folder in the project rather than to object storage.",
        ],
        featured: true,
        order: 9,
    },
    {
        slug: "advanced-tutorial-centre-system",
        title: "Advanced Tutorial Centre System",
        description:
            "A Java Swing desktop app that manages tutorial centre operations end-to-end — four role-specific interfaces over a shared object model with file-based persistence.",
        longDescription:
            "A desktop application built with Java Swing (laid out with IntelliJ's UI Designer), structured as a clear split between a backend package of domain classes — admin, receptionist, tutor, student, and a shared data manager — and a frontend package holding one window per role behind a single login screen. There is no database: the data manager reads and writes plain text files for students, tutors, receptionists, admins, classes, subject enrolments, payment requests, receipts and student requests, and every screen goes through it rather than touching storage directly.",
        role: "University group assignment, four contributors",
        technologies: ["Java", "Swing", "OOP", "File I/O"],
        githubUrl: "https://github.com/zhongxuen/Advanced-Tution-Centre-Management-Java",
        githubRepo: "Advanced-Tution-Centre-Management-Java",
        keyFeatures: [
            "Four role-specific Swing interfaces — admin, receptionist, tutor and student — behind one login screen",
            "Domain model split from the UI: a backend package of role classes plus a shared data manager",
            "Student, tutor and subject-enrolment management",
            "Payment requests and receipt records handled by the receptionist role",
            "File-based persistence — every screen reads and writes through the shared data manager, not directly",
        ],
        disclaimers: [
            "University group assignment built by four contributors, not a solo project.",
            "Desktop-only Java with no installer or live demo — you run it from an IDE. The screens are .form files from IntelliJ's GUI Designer, so building them expects IntelliJ.",
            "Persistence is plain text files committed alongside the code. There is no database, no concurrency control and no encryption — credentials sit in users.txt in the clear.",
            "It is coursework written to a module brief, not something to run a real tuition centre on.",
        ],
        featured: true,
        order: 10,
    },
    {
        slug: "education-management-system",
        title: "Education Management System",
        description:
            "A menu-driven Python CLI for managing student records, with full CRUD operations backed by file-based storage.",
        longDescription:
            "A console application split into one module per role — administration, staff, teacher and student — dispatched from a single entry point after login. Each role owns its own text-file store: course and class schedules, finance and instructor records for administration; resources, events and communication logs for staff; attendance for teachers; enrolments, grades, materials and feedback for students. There is no database or third-party library; persistence is plain file I/O.",
        role: "University group assignment, four contributors",
        technologies: ["Python", "File I/O"],
        githubUrl: "https://github.com/zhongxuen/Education-Management-System-Python",
        githubRepo: "Education-Management-System-Python",
        keyFeatures: [
            "Role-based login for Admin, Staff, Teacher and Student accounts",
            "Menu-driven CLI dispatched from one entry point into a module per role",
            "CRUD over course schedules, enrolments, grades, attendance and finance records",
            "Plain text-file persistence — no database and no external dependencies",
        ],
        disclaimers: [
            "University group assignment built by four contributors, not a solo project.",
            "A terminal application — there is no GUI, no web interface and no live demo. It runs with python main.py.",
            "Accounts and records are plain text files in the repository. Passwords are not hashed and there is no real authentication, so it is a teaching exercise rather than a system for real student data.",
        ],
        featured: false,
        order: 11,
    },
    {
        slug: "jommakan-website-interface",
        title: "JomMakan Website Interface",
        description:
            "A single-file restaurant-management UI covering dashboard, POS, reservations, inventory, CRM, staff, and promotions — frontend-only, with an optional Firebase fallback.",
        longDescription:
            "The entire interface is one HTML file: Tailwind from a CDN, vanilla JavaScript, and a client-side view switcher that swaps the content pane without a router or a build step. It is deliberately presentation-only — the brief was the interface, not the system behind it — so figures, charts and rosters run on mock data held in memory, and the sales chart is an explicit placeholder rather than a real plot. A Firebase and Firestore integration is scaffolded behind a config check: when credentials are present it initialises, and when they are not it falls back to mock-data mode instead of failing.",
        technologies: ["HTML", "Tailwind CSS", "JavaScript", "Firebase"],
        githubUrl: "https://github.com/zhongxuen/JomMakan-Website-Interface",
        githubRepo: "JomMakan-Website-Interface",
        screenshots: ["/images/projects/jommakan-dashboard.jpg"],
        keyFeatures: [
            "Seven-view sidebar navigation: Dashboard & Reports, POS & Billing, Reservations, Inventory Tracking, Customer CRM, Staff & Scheduling, Promotions & Loyalty",
            "Dashboard with sales, reservation, table-occupancy and low-stock summaries plus a top-sellers panel",
            "Inventory CRUD demo running against in-memory mock state",
            "Staff roster with duty status indicators and a performance summary table",
            "Promotions and loyalty program management table",
            "Optional Firebase/Firestore integration scaffold with automatic fallback to mock-data mode",
            "Ships as one self-contained HTML file — no build step, no bundler, no server",
        ],
        disclaimers: [
            "Interface only. The brief was the front end, not the system behind it, so every figure, chart and roster runs on mock data held in memory and nothing is saved — a refresh resets it.",
            "The sales chart is a placeholder graphic, not a plot of the numbers next to it.",
            "The Firebase and Firestore integration is scaffolded but not configured. With no credentials present it falls back to mock-data mode, which is how the file actually runs.",
            "Tailwind is loaded from a CDN at runtime, so the page needs a network connection to look right.",
        ],
        featured: false,
        order: 12,
    },
    {
        slug: "travel-expertise-ai-chatbot",
        title: "Travel Expertise AI Chatbot",
        description:
            "A Botpress travel-assistant chatbot with structured conversation flows and intent handling for trip-planning queries.",
        technologies: ["Botpress"],
        keyFeatures: [
            "Conversational flow design",
            "Intent handling",
            "Structured dialogue system",
        ],
        disclaimers: [
            "Built inside Botpress's hosted studio rather than in code, so there is no source repository and no deployed link to open here.",
            "Flow-and-intent design, not a language model of my own: the conversation is authored, and the reasoning is Botpress's.",
        ],
        featured: false,
        order: 13,
    },
];
